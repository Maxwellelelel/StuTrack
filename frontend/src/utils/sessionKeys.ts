/*
    Keeping all keys stored in a single file where they are visible is likely terrible for security,
    but it makes managing the keys clearer and decreases repetitive use in code.

    In the future I will have to find a better solution so that the security will also be enhanced...
 */

export const KEY_USER_LOGGED_IN = "key-logged-in"